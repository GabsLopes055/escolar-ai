import { MouthCardPipe } from './mouth-card.pipe';

describe('MouthCardPipe', () => {
  it('create an instance', () => {
    const pipe = new MouthCardPipe();
    expect(pipe).toBeTruthy();
  });
});
